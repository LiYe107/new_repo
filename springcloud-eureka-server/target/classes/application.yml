server:
  port: 8000
eureka:
  instance:
    hostname: localhost
    #hostname: peer1
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8000/eureka
      #defaultZone: http://peer2:8002/eureka